# A4_CHANGELOG (履歴管理)

## [2025-12-25] Run1: だるまUIパック（非破壊）
### 追加機能・変更点
- **JST日付判定の導入**: `getJSTNow()` を追加し、ブラウザの地域設定に関わらず日本時間で判定するように変更。
- **期間の更新**: 運用期間を 2025/12/26 〜 2026/01/07 (13日間) に固定。
- **だるまテーマ適用**: タイトルを「🎍 だるまの にこにこカレンダー」に変更。サブコピーをお正月仕様に切り替え。
- **スタンプ判子枠**: CSSにより😊🙂😢を円形の判子枠に変更し、選択時の沈み込みアニメーションを実装。
- **福バッジ**: 当日の2回完了を条件に「福」バッジを表示するロジックを追加。
- **導線A (おひっこし案内)**: 12/26以降の初回起動時に表示されるダイアログを実装。

## [2025-12-25] Run2: シーズンリセット（seasonId）
### 追加機能・変更点
- **自動シーズンリセット**: だるまシーズン開始日（12/26）以降の初回起動時に、進捗データを自動的に初期化するロジックを実装。
- **二重発火防止 (seasonId)**: `daruma_2025_12_26` を seasonId として導入。
  - 未ログイン時: localStorage (`santa_season_id`) に保存。
  - ログイン時: Supabase Auth の `user_metadata` に保存し、マルチデバイスでの整合性を確保。
- **リセット対象の限定**: スタンプ。おてつだい進捗のみを初期化し、音設定やテーマ設定、既読フラグなどは保持。

## [2025-12-25] Run2.5: クリスマス残骸掃除（非破壊）
### 追加機能・変更点
- **UI文字列の「だるま/お正月」化**: タイトル、h1、おてつだいセクションの「サンタスタンプ」等の文言を「だるま/ごほうび」へ置換。
- **画像アセットの刷新**:
  - スゴロク進捗アイコンを `daruma_stamp.png` へ変更。
  - ボーナス画像を `bonus-daruma.png` へ変更。
- **非破壊置換の徹底**: 内部的な localStorage キー（`santa_` プレフィックス）は破壊防止のため維持。

## [2025-12-25] Run2.6: お正月UIスキン（和紙ミニマル）
### 追加機能・変更点
- **和紙ミニマルテーマの導入**: CSS変数による「生成り・朱・金」の配色設計への一元化。
- **背景装飾**: `assets/washi_pattern.svg` を用いた和紙風テクスチャ背景の実装。
- **透かし漢字**: 画面の右上・左下に『謹賀新年』の透かし文字を配置（非インタラクティブ）。
- **タイトル・カード装飾**:
  - タイトル下に金の境界線。
  - 日付カードやボタン類を朱色の枠線で統一。
- **タイトル表記の統一**: タイトルおよびh1を『🎍 だるまの にこにこカレンダー』に修正。

### 影響範囲
- `styles.css` および `index.html` のスタイリング部分。
- 内部ロジックや `localStorage` のデータ構造、保存・リセット機能への影響はなし。

### 影響範囲（全体）
- UI表示、CSS、日付計算ロジック。
- 既存のDBデータ（Supabase/LocalStorage）への破壊적変更はなし。
